# portfolio-liketaplink

## Run Locally

1. Install dependencies:
   `npm install`
2. Configure Supabase:
   Create `.env.local` with:
   `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`
3. Run the app:
   `npm run dev`